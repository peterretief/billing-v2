\documentclass[a4paper,11pt]{article}
\usepackage[utf8]{inputenc}
\usepackage{booktabs}

\begin{document}

\section*{VAT Output Report: {{ month_name }} {{ year }}}

\begin{tabular}{ll}
    Total Net: & R {{ net_total }} \\
    Total VAT: & R {{ vat_total }} \\
\end{tabular}

\vspace{1cm}

\section*{Transaction Detail}
\begin{tabular}{llrrr}
    \toprule
    \textbf{Date} & \textbf{Invoice \#} & \textbf{Net} & \textbf{VAT} & \textbf{Total} \\
    \midrule
    {% for inv in invoices %}
    {{ inv.date_issued|date:"Y-m-d" }} & {{ inv.number }} & {{ inv.subtotal_amount }} & {{ inv.tax_amount }} & {{ inv.total_amount }} \\
    {% endfor %}
    \midrule
    % Use Django variables for the totals
    \textbf{TOTAL} & & \textbf{ {{ net_total }} } & \textbf{ {{ vat_total }} } & \textbf{ {{ gross_total }} } \\
    \bottomrule
\end{tabular}

\end{document}