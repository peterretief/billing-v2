\documentclass[a4paper,11pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[margin=1in]{geometry}
\usepackage{booktabs}
\usepackage{longtable}

\begin{document}

\centering

\textbf{\Large Time Detail Report}

Reference Invoice: \#{{ invoice_number }}

Client: {{ client_name }}

Date Generated: {{ date_generated }}

\vspace{0.5cm}

% Table columns: Date (left-aligned), Category (left-aligned), Description (left-aligned, 5cm width with text wrapping), Hours (left-aligned)
\begin{longtable}{llp{5cm}l}
\toprule
\textbf{Date} & \textbf{Category} & \textbf{Description} & \textbf{Hours} \\
\midrule
{% for entry in flat_entries %}
{{ entry.date|date:"d M Y" }} & {{ entry.category|default:"General" }} & {% if entry.metadata %}{{ entry.formatted_metadata }}{% else %}Standard work{% endif %} & {{ entry.hours }}h \\
{% endfor %}
\bottomrule
\end{longtable}

\vspace{0.5cm}

Detailed time report for Invoice \#{{ invoice_number }}.

\end{document}