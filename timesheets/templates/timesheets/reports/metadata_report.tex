\documentclass[a4paper,11pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[margin=1in]{geometry}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{longtable}

\definecolor{primary}{RGB}{40, 40, 40}

\begin{document}

\begin{flushleft}
    {\huge \textbf{Time Detail Report}} \\
    \vspace{2mm}
    \large \textcolor{primary}{Reference Invoice: \#{{ invoice_number }}} \\
    \large Client: {{ client_name }} \\
    \large Date Generated: {{ date_generated }}
\end{flushleft}

\vspace{10mm}

{% for category, entries in grouped_data.items %}
    % This creates the category header
    \section*{ {{ category|escape }} }
    \nopagebreak
    \begin{longtable}{p{2.5cm} p{10cm} r}
        \toprule
        \textbf{Date} & \textbf{Work Description \& Metadata} & \textbf{Hours} \\
        \midrule
        {% for entry in entries %}
            {{ entry.date|date:"d M Y" }} & \textbf{ {{ entry.description|escape }} } \\
            & \small 
            {% for key, val in entry.metadata.items %}
                \textit{ {{ key|escape }}: {{ val|escape }} } \quad
            {% endfor %}
            & {{ entry.hours }}h \\
            \addlinespace[2mm]
        {% endfor %}
        \bottomrule
    \end{longtable}
    \vspace{5mm}
{% endfor %}

\begin{flushright}
    \textbf{Grand Total Hours: {{ total_hours }}h}
\end{flushright}

\vfill
\begin{center}
    \footnotesize This report is a detailed breakdown of the hours billed on Invoice \#{{ invoice_number }}.
\end{center}

\end{document}