\documentclass[a4paper,11pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[margin=1in]{geometry}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{longtable}

\definecolor{primary}{RGB}{40, 40, 40}

\begin{document}

\begin{flushleft}
    {\huge \textbf{Time Detail Report}} \\
    \vspace{2mm}
    \large \textcolor{primary}{Reference Invoice: \#{{ invoice.number }}} \\
    \large Client: {{ invoice.client.name }} \\
    \large Date Generated: {{ invoice.date_issued|date:"d M Y" }}
\end{flushleft}

\vspace{10mm}

{% regroup timesheets by category as grouped_timesheets %}
{% for group in grouped_timesheets %}
    \section*{ {{ group.grouper.name }} }
    \begin{longtable}{lp{8cm}r}
        \toprule
        \textbf{Date} & \textbf{Details} & \textbf{Hours} \\
        \midrule
        {% for entry in group.list %}
            {{ entry.date|date:"d M Y" }} & 
            {% if entry.formatted_metadata %}
                {{ entry.formatted_metadata }}
            {% else %}
                Standard work
            {% endif %}
            & {{ entry.hours }}h \\
        {% endfor %}
        \bottomrule
    \end{longtable}
    \vspace{5mm}
{% endfor %}

\vfill
\begin{center}
    \footnotesize Detailed time report for Invoice \#{{ invoice.number }}.
\end{center}

\end{document}
